# Data directory

Raw public data is downloaded at runtime and is intentionally not committed.

Run:

```bash
python scripts/download_data.py
```

Expected local file:

```text
data/ai4i2020.csv
```

Source citation and dataset rationale are documented in `docs/dataset.md`.
